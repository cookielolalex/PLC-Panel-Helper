# Render Sheetmetal Drawing

Use only after `drawing_model` is schema-valid and frozen for the run.

Render `02_鈑金施工圖_<工程編號>.pdf` from the shared model. Include supported
fabrication dimensions, materials/thickness, folds/bends when source-supported,
and constructability notes. Missing critical values remain unresolved.

