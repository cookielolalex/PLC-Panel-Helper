# Evidence Provenance

Use when extracting values from approved current-project evidence into
`job_spec`.

Do not use to resolve conflicts from forbidden references.

Every extracted or derived value keeps file ID, sheet ID, cell/range or row,
raw displayed value, normalized value, confidence, verification status, and rule
ID when derived.

