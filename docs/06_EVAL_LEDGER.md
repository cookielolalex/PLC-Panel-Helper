# Evaluation Ledger

No real evaluation runs yet.

Synthetic harness run IDs will be recorded under `evals/runs/` only after the
Phase 0 tests create immutable run manifests and trajectories.

Source inventory is not an evaluation run. The D-0003 inventory created
manifest evidence only and did not expose references or scores to a generator.
