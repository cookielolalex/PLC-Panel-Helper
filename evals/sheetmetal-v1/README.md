# Sheetmetal V1 Evaluation Namespace

This namespace is reserved for the sheetmetal-first modular evaluation system.
The migration task adds only synthetic fixtures and does not begin a real
project baseline.

Current synthetic fixture:

- `evals/fixtures/sheetmetal-v1/complete_pipeline_fixture.json`

Current regression entrypoint:

- `scripts/run_tests.py`

