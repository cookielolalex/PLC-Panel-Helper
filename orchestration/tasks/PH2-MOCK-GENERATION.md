# PH2-MOCK-GENERATION

Agent type: mock_case_generator.

Run ID: `RUN-1110104-AUTO-EVAL-002`.

Scope: one blind historical mock generation for project `1110104` using only
the verified `ALLOWED_EVAL` sanitized bundle.

Forbidden inputs: completed references, completed drawing names or hashes,
reviewer findings, expected dimensions, reference thumbnails, and raw source
workbooks.

Required outputs: generation completion record, process metadata, freeze
manifest, artifact hashes, leakage scan, and local ignored generated PDFs.
