You are a fresh root coordinator continuing this repository.

Read the canonical checkpoint files, `reports/calibration-006/independent_audit.json`, and `reports/evaluator-sensitivity/subagent_independent_audit.json`.
Current status: `PRE_SOURCE_SCREENING_BLOCKED_BY_REFERENCE_PRESENCE`.
Six-project set: 1110101, 1110104, 1110204, 1110205, 1110405, 1110410.
Evaluator version for cycle-000: `plc_layout_evaluator_v2_sensitivity`.
Baseline-024 seed: `BASELINE024-CYCLE000-20260623`.

The user explicitly authorized cycle-000 in the current thread. Phase A found and fixed an evaluator-mechanics defect: original `42/38` was hardcoded, while v2 recomputes the same `42/38` from explicit scoring records and passed independent sensitivity audit.

Phase B froze `docs/specs/24_PROJECT_BASELINE_PROTOCOL.md` and `evals/baseline-024/*` protocol manifests. Six calibration anchors are retained as fresh-run anchors.

Phase C batch-001 accepted `1110801` and `1120207` as evaluation-only additions after independent source-bundle audit. `1120101`, `1120204`, and `1120301` were rejected by bundle verification; `1120201` remained quarantined/no-bundle.

Phase C batch-002 accepted `1110103`, `1110203`, and `1120308` as evaluation-only additions after independent source-bundle audit. `1110404` and `1120202` were rejected by bundle verification; `1120309` remained quarantined/no-bundle.

Phase C batch-003 accepted `1110704` and `1120305` as evaluation-only additions after independent source-bundle audit. `1110701` was rejected by bundle verification; `1110402`, `1110706`, and `1120102` remained no-bundle/quarantined.

Phase C batch-004 accepted no new evaluation-only additions after independent source-bundle audit. `1110501` was rejected by bundle verification; `1110504` remained quarantined/no-bundle.

Final Phase C result before amendment: the frozen metadata-only candidate pool was exhausted. All 20 candidates were processed, only 7 produced accepted sanitized bundles, and current baseline-024 `ALLOWED_EVAL` count is `13 / 24`. Eleven additional source-approved projects are still required, so no 24-project generation is authorized.

Amendment `D-0017` accepted on 2026-06-23 expands baseline-024 candidate discovery beyond the prior twenty-project metadata-only pool to the full approved development inventory under `SRC-ALL-PROJECTS`. It supersedes only the discovery cap and preserves source immutability, source-root restrictions, positive source allowlisting, evaluation-only quorum, reference isolation, cohort isolation, held-out protection, parser requirements, sanitized-bundle verification, independent audit, frozen workflow, grading, and no-invention gates.

Expanded screening progress after amendment:

- `065be52` reconciled the full project universe: 404 project IDs and 404 physical project folders.
- `49d5ba6` added metadata target detection v2 without opening completed-reference contents.
- `f9c58d0` built `expanded_candidate_registry_v2`; it found 0 immediate `READY_FOR_SOURCE_SCREENING` projects and 269 `REFERENCE_PRESENCE_REVIEW_REQUIRED` projects.
- `ed71617` and `d6c2333` ran three isolated reference-presence waves across 18 top-ranked partial-reference projects. All remained `PARTIAL_REFERENCE_SET` with only sheet-metal target evidence; 0 projects were promoted to all-three reference availability.

Exact next action: do not start source-review quorum, sanitized bundle construction, baseline generation, review, or optimization. Resume at the reference-presence layer only: continue isolated reference review with a more capable auditable classifier, or propose and independently review a deterministic target detector improvement with regression coverage. Do not weaken the all-three completed-target requirement and do not use forbidden production-control or electrical PDFs as target evidence.

Do not optimize the drawing workflow unless separately authorized through proposal-first rules.
