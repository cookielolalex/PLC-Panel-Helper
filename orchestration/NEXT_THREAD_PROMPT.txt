You are a fresh root coordinator continuing this repository.

Read the canonical checkpoint files, especially:

- `docs/01_CURRENT_STATE.md`
- `docs/04_DECISION_LEDGER.md`
- `docs/06_EVAL_LEDGER.md`
- `docs/SESSION_CHECKPOINT.md`
- `docs/specs/AUTONOMOUS_QUALIFICATION_RECOVERY.md`
- `reports/baseline-024/qualification-recovery/recovery_state.json`
- `orchestration/QUALIFICATION_RECOVERY_QUEUE.json`

Current status:

`DETECTOR_V4_LOCAL_CALIBRATION_IN_PROGRESS`

Starting point for verification:

- Git HEAD must be the latest checkpoint commit or a documented descendant.
- Full repository tests must pass before new implementation.
- Accepted bundle hashes and frozen workflow hashes must still pass.
- `docs/PRIVACY_APPROVAL.md` remains `NOT_APPROVED`.
- `SRC-ALL-PROJECTS` remains read-only by policy.
- Baseline generation attempts must remain `0`.
- No completed reference, page render, title-block crop, raw extracted text,
  private source path, or reference-derived engineering content may be visible
  to generators, source reviewers, production Knowledge, or optimization
  workspaces.

Current verified `ALLOWED_EVAL` projects remain `13 / 24`; deficit `11`; no
reserves are accepted.

Important distinction:

- `test_reference_detector_v3_known_positive_recall_gate` passes because it
  correctly preserves and blocks a failing detector result.
- Detector v3 performance itself failed: known-positive all-three recall `0 /
  13`, `PRODUCTION_DRAWING 0 / 13`, `SHEETMETAL_DRAWING 8 / 13`,
  `PUNCH_DRAWING 0 / 13`, false-negative output-type count `31`.

Decision `D-0021` authorizes constraint-preserving autonomous qualification
recovery. It supersedes `VISION_CLASSIFIER_UNAVAILABLE` as a terminal workflow
stop, but does not authorize GPT-5 or external/private-reference vision use.
Privacy approval remains `NOT_APPROVED`.

Local capability discovery result:

- Poppler `pdfinfo 26.05.0`: available locally.
- Poppler `pdftoppm 26.05.0`: available locally.
- Python `pypdf 6.10.0`, Pillow `12.2.0`, NumPy `2.3.5`: available locally.
- Windows.Media.Ocr: available through local Windows runtime probe.
- `pdfimages`, Tesseract, OCRmyPDF, PaddleOCR, EasyOCR, ONNX Runtime, OpenCV,
  PyMuPDF, and ImageMagick: unavailable in the local probe.
- No private project data was opened and no network endpoint probe was
  performed.

Queue state:

- prior v3 partial projects: `129`
- projects not individually screened by v3: `262`
- known-positive detector retry cases: `13`
- previous bundle rejections: `7`
- previous quarantined/no-bundle projects: `6`

Detector v4 state:

- v4 calibration protocol is frozen.
- implementation-facing calibration IDs are in
  `manifests/reference_detection/calibration/v4_calibration_manifest.json`.
- sealed holdout IDs are in a separate auditor-only manifest and must not be
  used for tuning.
- Windows.Media.Ocr synthetic proof passed locally; private-page OCR probe was
  skipped because no enforceable per-process network-disable boundary was
  available.
- v4 detector, v4 schemas, verifier, and regression tests are implemented.
- full repository tests pass.

Exact next action:

Run `RUN_DETECTOR_V4_CALIBRATION_PARTITION` using only the implementation-facing
calibration manifest and local-only v4 detector. Do not start source-review
quorum, sanitized-bundle construction, cohort freeze, baseline generation,
review, optimization, sealed holdout, or expanded corpus screening until the
calibration partition has frozen minimized evidence and the next gate permits
it.
