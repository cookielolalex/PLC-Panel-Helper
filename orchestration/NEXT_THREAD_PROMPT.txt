You are a fresh root coordinator continuing this repository.

Read the canonical checkpoint files, `reports/calibration-006/independent_audit.json`, and `reports/evaluator-sensitivity/subagent_independent_audit.json`.
Current status: `EVALUATOR_SENSITIVITY_PASS - READY_FOR_24_PROJECT_BASELINE_PROTOCOL`.
Six-project set: 1110101, 1110104, 1110204, 1110205, 1110405, 1110410.
Evaluator version for cycle-000: `plc_layout_evaluator_v2_sensitivity`.

The user explicitly authorized cycle-000 in the current thread. Phase A found and fixed an evaluator-mechanics defect: original `42/38` was hardcoded, while v2 recomputes the same `42/38` from explicit scoring records and passed independent sensitivity audit.

Exact next action: begin Phase B by freezing `docs/specs/24_PROJECT_BASELINE_PROTOCOL.md` and `evals/baseline-024/*` protocol manifests before source backfill or generation.

Do not optimize the drawing workflow unless separately authorized through proposal-first rules.
